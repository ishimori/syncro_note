# DD-016-3: 右パネル編集UI（アジェンダ・参加者・事前資料）＋状態結線＋抽出プレビュー

| 作成日 | 更新日 | ステータス |
|--------|--------|------------|
| 2026-06-09 | 2026-06-09 | 完了（資料追加の通しE2Eのみ別途） |

> 親: [DD-016](DD-016_S-05リアルタイム画面の改善_録音中の右パネル編集と会話エリア独立スクロールとヘッダ固定.md)。アプローチ: モック先行（合意済みモック `DD-016/mock/S-05_realtime_v2.html` が正）。**依存: [DD-016-2](DD-016-2_adhoc仮会議のライフサイクルと保存拡張と未保存スイープ.md)（仮会議コマンド）**。

## 目的

「今すぐ録音（ad-hoc）」でも、録音中に右パネルから会議の前提をその場で登録・編集できるようにする（案C）。

1. **アジェンダ**: その場でテキスト編集。
2. **参加者**: 名前＋役職の追加・削除。追加した人を**話者確定プルダウンへ反映**（ad-hoc で話者を割り当て可能に）。
3. **事前資料**: 録音中に追加・削除。解析状態を表示し、**追加資料も清書(S-06)に反映**。各資料の**「確認」ボタンで抽出テキストをダイアログ表示**（清書に渡るのと同じ内容・コピー可）。

## 背景・課題

現状の右パネルは読み取り専用で、予定(?id=)を開いた時だけ実データが入る。ad-hoc では `（未設定）/（登録なし）/（なし）` のまま編集できず、話者確定プルダウンも空になる（親DD背景参照）。

## 検討内容

- **アジェンダ・参加者**: `minutesSession`（S-05→S-06→S-07 縦串状態）に `agenda`/`participants` を追加して保持。保存時、仮会議が有れば `complete_meeting`（DD-016-2 拡張）・無ければ `create_meeting` に渡す。
- **事前資料**: 追加時に仮会議を確保（DD-016-2 の仮会議作成→`minutesSession.meetingId` 設定）→ 既存 `add_attachment`／`list_attachments`／`remove_attachment` を使用。解析状態（pending/done/error）と本文有無を表示。
- **抽出プレビュー**: S-02(`S02CreateMeeting.vue`)/S-03 の既存作法（`canPreview`/`openPreview`/`q-dialog` ＋「AIの清書に渡るのと同じ内容です」＋コピー）を流用し画面間で統一。
- **離脱時掃除**: 保存せず S-05/S-06 を離脱したら仮会議を削除（DD-016-2 の削除/既存 `delete_meeting` を `onUnmounted` 等で呼ぶ）。

## 決定事項

- 合意済みモックの右パネル（編集UI＋抽出プレビュー）をそのまま `S05Realtime.vue` に移植。
- 保存分岐: 仮会議有り＝`complete_meeting`（agenda・participants含む）／無し＝`create_meeting`（agenda・participants付き）。

## タスク一覧

### Phase 1: 実装
- [x] `app/src/session.ts`: `agenda` / `participants`（SessionParticipant: name・role）／`isTempMeeting` を追加（初期化・reset 込み）
- [x] `app/src/pages/S05Realtime.vue`: 右パネルを編集可能化（アジェンダ＝textarea／参加者＝名前+役職の追加・削除→話者確定メニューは `participantLabels` 参照／事前資料＝`pickAndAttach`で追加・`removeAttachmentRow`で削除・解析状態表示・**抽出プレビューダイアログ**）。`ensureMeeting` で最初の添付時に仮会議を遅延作成し `minutesSession.meetingId`/`isTempMeeting` を設定。`endMeeting` で agenda/participants をセッションへ渡し、仮会議へ `updateMeeting` で書き戻し
- [x] `app/src/pages/S07MinutesPreview.vue`: ad-hoc 新規作成(`create_meeting`)に agenda・participants を渡す（仮会議/予定有り＝`complete_meeting` は従来どおり）
- [x] `app/src/pages/S06Generating.vue`/`S05Realtime.vue`: 保存せず離脱時に仮会議を削除（S-05 onUnmounted: 進行中でなければ削除／S-06 onUnmounted: 未完了かつ isTempMeeting なら削除）
- [x] 🔬 **機械検証**: `cd app && npx vue-tsc --noEmit` → 型エラー0
- [x] 🖥️ **実機確認**（CDP・localhost:9222 の実Tauriウィンドウ）:
  - 右パネルが実ランタイムで編集可能表示（アジェンダ入力／参加者 名前+役職+追加／資料を追加ボタン有効／清書反映の案内）→ 参加者「鈴木（PM）」追加が反映。エビデンス `DD-016-3/dd016-3-realwindow-editable-panel.png`
  - 一覧除外（DD-016-2 連携）を実DBで確認: `invoke create_meeting`（active・scheduled_end=null）→ `list_meetings` に出ない／`get_meeting_detail` では存在（DBにはある）／シードの active「本日の定例」は一覧に残る。確認後 `delete_meeting` で後始末
  - 制約: 資料追加は OS ネイティブのファイル選択ダイアログのため CDP 自動操作の対象外。チェーンの構成要素（`create_meeting` 実DB・`add_attachment` は既存 DD-012-10 の不変コード）は個別に実証済み。サンプル xlsx/pdf が無いため抽出の実走と清書反映の通し E2E は未実施（要 実ファイル＋3〜6分の清書）
- [x] 😈 **DA批判レビュー**: 下記

## ログ

### 2026-06-09
- 親DD-016から分割して作成（FE結線＝右パネル編集＋抽出プレビュー）。DD-016-2 のコマンドに依存。
- 実装完了。session.ts/S05/S06/S07 を変更し vue-tsc 0エラー。実Tauriウィンドウ(CDP)で編集パネル動作・参加者追加・一覧除外を実DB確認。レビューなし一括方針につきユーザーレビューゲートは省略。BE は新コマンド不要（既存 create/update/complete/delete_meeting + add/remove_attachment を再利用）。

---

## DA批判レビュー記録

<!-- 手順・品質フィルターは doc/da-method.md を参照 -->

### Phase 1 DA批判レビュー

**DA観点:** 保存分岐・仮会議結線で最も壊れやすい点は？

| # | 発見した問題/改善点 | 重要度 | 再現手順（高/中は必須） | DA観点 | 対応 |
|---|-------------------|--------|----------------------|--------|------|
| 1 | 仮会議作成後にアジェンダ/参加者を編集すると、作成時の値しか保存されない恐れ | 中 | 資料追加(仮会議作成)→その後 参加者を追加→会議終了→保存 | 状態の取りこぼし | ✅修正済（`endMeeting` で仮会議へ `updateMeeting` し最新値を書き戻す） |
| 2 | S-06 で清書を中断（未完了離脱）すると仮会議が幽霊化 | 中 | ad-hoc＋資料追加→会議終了→S-06 で中断/離脱 | 未保存の幽霊 | ✅修正済（S-06 onUnmounted で `isTempMeeting` なら `deleteMeeting`。取りこぼしは起動時スイープ＝DD-016-2 が拾う） |
| 3 | linked（予定）会議で participants を上書きすると話者リンクを壊す懸念 | 低 | 予定を開いて録音→参加者編集→保存 | データ整合 | ❌不要（linked は `complete_meeting` 経路で participants を触らない。ad-hoc/仮会議のみ書き込む設計） |
| 4 | 資料追加の実E2E（抽出→清書反映）が未走 | 中 | 実 xlsx/pdf を添付→会議終了→清書 | 検証不足 | ⏭️別途（ネイティブダイアログ＋実ファイル＋清書数分が必要。構成要素は実証済み・実ファイル入手後に通し確認） |
