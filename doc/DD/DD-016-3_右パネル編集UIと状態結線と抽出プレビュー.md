# DD-016-3: 右パネル編集UI（アジェンダ・参加者・事前資料）＋状態結線＋抽出プレビュー

| 作成日 | 更新日 | ステータス |
|--------|--------|------------|
| 2026-06-09 | 2026-06-09 | 未着手 |

> 親: [DD-016](DD-016_S-05リアルタイム画面の改善_録音中の右パネル編集と会話エリア独立スクロールとヘッダ固定.md)。アプローチ: モック先行（合意済みモック `DD-016/mock/S-05_realtime_v2.html` が正）。**依存: [DD-016-2](DD-016-2_adhoc仮会議のライフサイクルと保存拡張と未保存スイープ.md)（仮会議コマンド）**。

## 目的

「今すぐ録音（ad-hoc）」でも、録音中に右パネルから会議の前提をその場で登録・編集できるようにする（案C）。

1. **アジェンダ**: その場でテキスト編集。
2. **参加者**: 名前＋役職の追加・削除。追加した人を**話者確定プルダウンへ反映**（ad-hoc で話者を割り当て可能に）。
3. **事前資料**: 録音中に追加・削除。解析状態を表示し、**追加資料も清書(S-06)に反映**。各資料の**「確認」ボタンで抽出テキストをダイアログ表示**（清書に渡るのと同じ内容・コピー可）。

## 背景・課題

現状の右パネルは読み取り専用で、予定(?id=)を開いた時だけ実データが入る。ad-hoc では `（未設定）/（登録なし）/（なし）` のまま編集できず、話者確定プルダウンも空になる（親DD背景参照）。

## 検討内容

- **アジェンダ・参加者**: `minutesSession`（S-05→S-06→S-07 縦串状態）に `agenda`/`participants` を追加して保持。保存時、仮会議が有れば `complete_meeting`（DD-016-2 拡張）・無ければ `create_meeting` に渡す。
- **事前資料**: 追加時に仮会議を確保（DD-016-2 の仮会議作成→`minutesSession.meetingId` 設定）→ 既存 `add_attachment`／`list_attachments`／`remove_attachment` を使用。解析状態（pending/done/error）と本文有無を表示。
- **抽出プレビュー**: S-02(`S02CreateMeeting.vue`)/S-03 の既存作法（`canPreview`/`openPreview`/`q-dialog` ＋「AIの清書に渡るのと同じ内容です」＋コピー）を流用し画面間で統一。
- **離脱時掃除**: 保存せず S-05/S-06 を離脱したら仮会議を削除（DD-016-2 の削除/既存 `delete_meeting` を `onUnmounted` 等で呼ぶ）。

## 決定事項

- 合意済みモックの右パネル（編集UI＋抽出プレビュー）をそのまま `S05Realtime.vue` に移植。
- 保存分岐: 仮会議有り＝`complete_meeting`（agenda・participants含む）／無し＝`create_meeting`（agenda・participants付き）。

## タスク一覧

### Phase 1: 実装
- [ ] 📐 **実装前詳細化** → 👀 レビュー（触る関数・データフロー・保存分岐の before/after・仮会議確保のタイミング）
- [ ] `app/src/session.ts`: `agenda` / `participants`（名前・役職）を追加
- [ ] `app/src/pages/S05Realtime.vue`: 右パネルを編集可能化（アジェンダ＝テキスト編集／参加者＝追加・削除→話者確定メニューへ反映／事前資料＝追加・削除・解析状態・**抽出プレビューダイアログ**）。事前資料追加時に仮会議を確保し `minutesSession.meetingId` を設定
- [ ] `app/src/pages/S06Generating.vue`/`S07MinutesPreview.vue`: 保存分岐（仮会議有り＝`complete_meeting`／無し＝`create_meeting`、どちらも agenda・participants を渡す）
- [ ] `app/src/pages/S05Realtime.vue`/`S06`: 保存せず離脱時に仮会議を削除（`onUnmounted` 等）
- [ ] 🔬 **機械検証**: `cd app && npx vue-tsc --noEmit`（型エラー0）
- [ ] 🖥️ **実機確認**: `node scripts/tauri-cdp.mjs` で ad-hoc 録音→参加者/アジェンダ編集→資料追加→**抽出プレビュー**→会議終了→清書に資料が反映→保存。スクショを `DD-016-3/` にエビデンス
- [ ] 😈 **DA批判レビュー**（保存分岐の取り違え・仮会議の二重確保・離脱時掃除の取りこぼし・linked 会議への影響・最低1件）

## ログ

### 2026-06-09
- 親DD-016から分割して作成（FE結線＝右パネル編集＋抽出プレビュー）。DD-016-2 のコマンドに依存。

---

## DA批判レビュー記録

<!-- 手順・品質フィルターは doc/da-method.md を参照 -->

### Phase 1 DA批判レビュー

**DA観点:** （保存分岐・仮会議結線で最も壊れやすい点は？）

| # | 発見した問題/改善点 | 重要度 | 再現手順（高/中は必須） | DA観点 | 対応 |
|---|-------------------|--------|----------------------|--------|------|
| 1 | (具体的に記述) | 高/中/低 | (高/中: 操作→結果) | (DA観点) | ✅修正済/⏭️別DD/❌不要 |
