# DD-017: テレビデモモード（ライブ文字起こし＋完全オフライン実証＋話者分けの見せ場化）

| 作成日 | 更新日 | ステータス |
|--------|--------|------------|
| 2026-06-09 | 2026-06-09 | 完了（017-1 既存充足/017-2/3/4 実装・実機実証ずみ。残=多話者k可変は将来別DD） |

> アプローチ: 親DD（管理用）。実装は子DD（DD-017-1〜3）で行う。レビューゲートが複数のため分割。

## 目的

他社へナナイロの開発手法（DD＋複数LLM運用）を提示する**ショーケース用のデモ体験**を仕立てる。テレビ（ニュース／ワイドショー）の音声を入力に使い、「**録音中にライブで文字起こし＋話者を色分け（ライブ逐次）→清書**」を、**完全オフライン（機内モード）であることを画面で証明**しながら見せられる状態にする。

主役はあくまで `doc/Slides/SynchroniNote_紹介スライド.md` の主張（手法の再現性・完全ローカルの安心）。本DDはその主張を**口頭でなく実演で裏づける**ための見せ場づくり。

> **テレビモードは独立した「高負荷モード」**。通常の会議モード（ホットパス軽量・会議後一括）とは別物として扱い、デモ中は**マシンパワーをフル投入**して「①ライブ逐次の話者分離」「②正しい文字起こし」に全振りする。代償として**ライブのLLM整形・DB保存は割愛可**（デモは見せ場が主目的で常用ではないため）。

## 背景・課題

- このアプリは「ローカルLLMがどこまで実用かを測る物差し兼ショーケース」であり、実会議での常用は目的ではない（方針の合意済み）。評価軸は **DER（精度）ではなく「他社に刺さるか＝デモ映え」**。
- 客先で本物の機密会議は再現できない。一方**テレビ音声は日本語・複数人・実音声が無尽蔵**で、観客が自分の耳で答え合わせできる＝ヤラセ感のないライブデモ素材になる。
- 話者分離は**すでに本番経路に配線済み**（DD-012-5）。`pipeline/sidecar.py` が録音中は仮 `spk0`、**停止後に1回だけ** CAM++ 埋め込み（`diarization/labeling.py` の `onnx-embed`、モデル未配置時は `simple-cluster` に自動フォールバック）で分離し `{type:"speakers", map:{seq→spk}}` を emit。スクショの spk0/spk1 はその実出力。
- よって本テーマは「話者分離をゼロから作る」ではなく **①テレビ素材で実用に整える ②デモで主役に押し出す**。

## 検討内容

### 分かれ道①: ライブ逐次の話者割当をどう実現するか（→ ローリング再分離で入れる）

- 基本設計は**会議モードでは**重い diarization をホットパスから外す（`基本設計書.md` 原則5）。これは**通常会議モードの方針**として維持。
- 一方**テレビモードは独立した高負荷モード**。デモ中に限りフルパワーを許すので、ライブ逐次を入れる方針に転換（ユーザー指示 2026-06-09）。スライドの〈重い処理はホットパスから外す〉とは「**通常モードはそのまま／テレビモードは別物**」として両立させる。
- 実現方式の検討:
  - ❌ **真のストリーミング・クラスタリング（オンライン逐次）**: 録音中に未確定のまま話者を逐次割り当てる本格手法。早期は情報不足で不安定・話者数未知・実装難物。リスク高。
  - ✅ **ローリング再分離（採用）**: 数秒ごとに「それまでの全音声」をまるごと再分離（`diarize_for_labeling`）し、`seq→spk` を貼り直す。埋め込み抽出は **RTF 0.008** と激安なので長尺でも回せ、**聞くほど精度が上がる**。途中のラベル入れ替わりは「直前結果との整合をとる label 整列（ハンガリアン等）」で抑制。
- → **決定: ライブ逐次を採用。方式はローリング再分離**。保存割愛で「会議後一括」の制約も外れるため相性良い。

### 分かれ道④: 「正しく文字起こし」をどう底上げするか（テレビモードのフルパワー活用）

- DD-003 の核心は「**CPUでは STT と LLM を同時に回せない**（同時実行で whisper RTF 1.48 と破綻）」。会議モードはこれを時間分離で回避。
- テレビモードは**ライブLLM整形を止める**ので、その分のCPUを丸ごとSTTに回せる。これを使って:
  - STTモデルを **medium → 大型（large-v3 等）** に引き上げて文字起こし精度を上げる選択肢（RTF<1 を実機確認のうえ）。
  - もしくは medium のまま VAD/ビーム幅などデコード設定を精度寄りに振る。
- → 子DD-017-3 で実機計測して採否を決める（フルパワー時に RTF<1 を満たすか）。

### 分かれ道②: テレビは話者数が可変（現状 k=2 固定）

- `sidecar.py --speakers` の既定は **k=2 固定**。話者数の自動推定は DD-004-1 の積み残し。
- テレビ（特にワイドショー）は 3〜5人と可変 → k=2 のままだと取りこぼす。デモ用に**人数を指定できる UI** か**自動推定**が要る（子DD-017-2 で扱う）。

### 分かれ道③: テレビ入力の経路

- YAMAHA マイク採用でテレビ音声を入力にできる見込み。物理マイク収音か OS ループバックかは子DD-017-1 で実機確認（長時間でドロップ0・遅延許容を満たすか）。

## 決定事項

- デモ方針＝**A（ライブ・完全オフライン実証のデモ体験）**＋**テレビモード＝独立した高負荷モード**。
- **ライブ逐次の話者分離を採用**（方式＝ローリング再分離）。録音中に話者色がライブ更新される。
- **テレビモード中はフルパワー投入**。代償として **ライブLLM整形・DB保存は割愛可**。これにより STT を大型モデルへ引き上げる余地を作る。
- 通常の会議モードは従来どおり（ホットパス軽量・会議後一括・保存あり）。テレビモードはトグルで切替える別経路。
- 本DDは親（管理用）。以下の子DDに分割して回す（各々が1レビューゲート）。
- 評価軸は**目視（デモ映え）**。テレビ素材に正解ラベルは無く DER 数値採点はしない（数値評価は DD-004-1 の正解付きクリップに任せる）。

## タスク一覧（子DDの管理）

> 親DDのタスク＝子DDの起票・進行管理。各子DDは起票時に Phase0（事前精査）から個別に回す。

- [x] **DD-017-1｜テレビ入力の取り込み** → **既存機能で充足（別途起票不要）**
  - テレビ音声の取り込みは**既存の mic 収音経路（DD-012-1）＋デバイス選択（DD-012-14）でそのまま成立**。専用実装は不要。
  - 実証: 実使用で S-05 がテレビ音声を文字起こし済（アジェンダ「テレビの音声」・~4分経過・**drop 0**）＝長時間ドロップ0・遅延許容内を実機で確認済。
  - 物理収音/ループバックの違いは OS 既定入力の選択（S-08/DD-012-14）で吸収。新規 DD は起こさない。
- [x] **DD-017-2｜ライブ逐次の話者分離（ローリング再分離）＋話者数可変** → [起票・実装済](DD-017-2_ライブ逐次の話者分離_ローリング再分離とラベル安定化.md)
  - `sidecar.py` を録音中 N 秒ごとの全バッファ再分離→`speakers` 逐次 emit に拡張（`--live-diarize`）。`stabilize_labels` で色の入れ替わりを抑制。
  - CLI実証: 33回ライブ emit・ラベル反転0・2話者安定。フロントは `stt-speakers` が冪等再マップ済みで無改修対応と確認。
  - 実機ライブ目視は 017-4 の通しE2Eで達成（録音中に色がライブ更新）。残: 多話者（3〜5人）での k 可変確認のみ（将来）。
- [x] **DD-017-3｜STT精度の引き上げ（フルパワー配分）** → [起票・計測完了](DD-017-3_STT精度の引き上げ_フルパワー配分.md)
  - 実測で確定: **ライブ=medium/threads8/beam1 が上限**（streaming RTF 0.973）。大型化(large-v3)は live 2.67/batch 3.12 で**不採用**、かつ chunk では精度優位が消滅。スレッド4→8（LLM停止で空けたコア）が live 成立の鍵。
  - 「正しくテキスト化」の本命は**停止後の medium batch 再起こし**（CER 0.40→0.27）。デモ演出として任意採用は 017-4 で判断。
  - 仮説「フルパワー＝大型で精度UP」は実測で否定（スライドの『実測が設計を上書きする』の好例）。
- [x] **DD-017-4｜デモUI＋テレビモード切替** → [起票・実装済](DD-017-4_デモUIとテレビモード切替_オフラインバッジ.md)
  - `start_mic(tv_mode)` で **--live-diarize＋threads8＋整形オフ**を1トグルに束ねる配線（cargo check 通過）。
  - S-05 に**テレビモードトグル**＋ヘッダに**オンライン/オフラインバッジ**（vue-tsc 通過・実ウィンドウHMRで表示目視）。方針A＝停止後きれい化パスは入れない。
  - **通しライブE2E実証ずみ**: `start-app.sh`再起動→テレビモードで録音→録音中(25s)に17セグメント・2話者(spk0/spk1)がライブ色分け＋オフラインバッジ切替を実機確認（[エビデンス](DD-017-4/s05-live-diarization-and-offline-badge.png)）。

## ログ

### 2026-06-09
- DD作成（親DD）。背景調査で「話者分離は DD-012-5 で会議後一括ラベリングとして既に本番配線済み（CAM++ onnx-embed＋simple-clusterフォールバック）」を確認。
- 初版は「ライブ逐次は不採用・終了で色分け」だったが、ユーザー指示で**方針転換**: テレビモードを独立した高負荷モードとし、**ライブ逐次の話者分離を採用（方式＝ローリング再分離）**＋**フルパワーでSTT精度を底上げ**＋**ライブLLM整形/DB保存は割愛可**。子DDを 017-1（入力）/017-2（ライブ逐次分離・k可変）/017-3（STT精度）/017-4（デモUI・モード切替）に再構成。
- **完了総括（漏れチェック後）**: 017-2/017-3/017-4 を起票・実装・実機実証して完了。017-1 は専用実装不要＝既存 mic 経路で充足（テレビ音声を~4分 drop0 で文字起こし実績）と確定。017-2 Phase3 の実機ライブ確認も 017-4 通しE2E で達成。**残るは多話者(3〜5人)の k 可変のみ**で、これは本DDのデモ目的（2話者で成立）の範囲外＝必要になれば将来別DDで起票する。→ **DD-017 系クローズ・アーカイブ**。

---

## DA批判レビュー記録

### 親DD DA批判レビュー

**DA観点:** ショーケース目的の作業が、スライド自身の主張（脱バイブコーディング・完全ローカル）と矛盾しないか？

| # | 発見した問題/改善点 | 重要度 | 再現手順 | DA観点 | 対応 |
|---|-------------------|--------|----------|--------|------|
| 1 | 「デモ映えのために測らず作り込む」と、スライドが批判するバイブコーディングそのものになる | 高 | テレビ向けにつまみを無計測で調整→会議用途が劣化 | 自己矛盾 | ✅ 各作業を子DDの計測ゲート（RTF/目視）経由に限定 |
| 2 | ライブ逐次 diarization が基本設計（ホットパス回避）と矛盾しCPU破綻リスク | 高 | 録音中に分離→STTと競合しドロップ | 設計整合 | ✅ **テレビモードを独立した高負荷モードとして分離**（通常会議モードは従来どおり）。ライブLLM整形を止めCPUを空ける。方式は激安なローリング再分離（RTF 0.008）。子DD-017-2/3 で RTF<1 を実機ゲート |
| 3 | テレビ素材には正解ラベルが無く、精度を数値で測れない（DER採点不可） | 中 | テレビ音声で自動採点しようとして不能 | 評価可能性 | ⏭️ 数値評価は DD-004-1 の正解付きクリップに任せ、本テーマは目視のみと割り切り |
| 4 | ローリング再分離でラベル（spk0/spk1…）が録音中に入れ替わり、画面の色がチラつく | 中 | 数秒ごと再分離→クラスタ順が変わり色が飛ぶ | UX/設計整合 | ⏭️ 子DD-017-2 で「直前結果との label 整列（ハンガリアン等）」を実装し抑制 |
| 5 | フルパワー時に大型STTモデルで RTF≥1 になり、ライブが追いつかず破綻 | 高 | large-v3＋ライブ分離を同時実行→遅延蓄積 | 性能 | ⏭️ 子DD-017-3 で RTF<1 を満たす構成（モデル/スレッド配分）を実機計測して確定。満たさなければ medium 据え置き |
