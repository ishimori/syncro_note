# DD-012-1: マイク収音をライブでUIに接続

| 作成日 | 更新日 | ステータス |
|--------|--------|------------|
| 2026-06-08 | 2026-06-08 | 完了 |

> 親DD: [DD-012 製品化（中核機能の実装と実用化）](DD-012_製品化_中核機能の実装と実用化.md)
> アプローチ: 標準（探索的実装。Tauriランタイム依存のため検証は実ウィンドウ＝Playwright不可）
> 位置づけ: DD-011 Phase 3-C（サンプル音声ファイルでの配線）の「マイク差分」。土台は 3-C を再利用し、入力源と連続収音の操作だけを足す。

## 目的

DD-011 Phase 3-C で「サンプル音声ファイル→S-05 に逐次表示」の配線（Rust中継／画面 listen／ダミー撤去／子プロセス kill）が通る。本DDはその入力源をサンプルファイルから**実マイク**へ差し替える差分。マイクで話した内容が S-05 に1行ずつ流れる状態にする。

## 背景・課題

- DD-011 Phase 3 は 3-A（画面シェル・完了）/3-B（Python実行口 sidecar.py・ファイル入力）/3-C（Rust中継＋S-05ライブ配線・ファイル入力）に分割済み（別セッション管轄）。配線の土台（Rust spawn→stdout中継→emit→kill、画面の listen→timeline.push、ダミー撤去）は **3-C が作る**。
- Python 側はマイク収音＋8〜12秒VADチャンク＋逐次STTが DD-010 で実装済み（`python/src/synchroni_note/realtime/`）。
- よって本DDは新規発明ではなく、**3-C の土台に「マイク入力」と「連続収音の操作（開始/一時停止/停止）」を足すだけ**。サイドカー契約（JSON Lines: `meta`/`segment`/`done`/`error`）は不変なので、画面・Rust中継はほぼそのまま再利用できる。

## 検討内容

- **3-C を再利用**: Rust の中継・kill、画面の listen・timeline.push・ダミー撤去は 3-C 成果をそのまま使う（本DDで作り直さない）。
- **差分は3点**: ① sidecar.py に mic モード（DD-010 の realtime 経路を JSON Lines で流す。3-B のファイル版に追記）② Rust の起動をファイル／mic で切替（spawn 引数の差）③ 画面に連続収音の操作（録音 開始/一時停止/停止）。ファイルは1回読み切り、マイクは止めるまで流れ続ける点が主な違い。
- マイクデバイスは S-04/S-08 の選択値を使用（無ければ既定デバイス）。

## スコープ

- **やる**: sidecar.py の mic モード追加、Rust の mic 起動対応、S-05 の録音 開始/一時停止/停止、実マイクでの逐次表示。
- **やらない（＝ DD-011 3-C 担当・再利用）**: Rust中継の新規実装・画面 listen 基盤・ダミー4件の撤去・子プロセス kill の新規実装。
- **やらない（別の子DD）**: 終了後の要約清書（012-2）／保存（012-3）／LLM追い上げ整形（012-4）／話者ラベル（012-5）。

## 依存

- 前提: **DD-011 Phase 3-C 完了**（ファイル入力で配線の土台が通っていること）。3-C 未完なら本DDは着手しない。
- 流用: **DD-010**（realtime 収音＋逐次STT）／**DD-011 3-B**（sidecar.py）。

## タスク一覧

### Phase 0: 事前精査 ✅
- [x] 📋 タスク精査。3-C 成果（Rust中継・画面listen・kill）の再利用範囲を確認＝そのまま再利用でOK
- [x] 📐 実装前詳細化（要点＝連続収音の停止制御）→ **stdin制御方式に確定**（`pause`/`resume`/`stop` を1行ずつ。stdin EOF でも停止）。実マイク無し検証用に **`--simulate`（feed_samplesでファイルをmic代替）** も用意
- [x] 😈 Devil's Advocate（マイク権限・デバイス・無音・長時間メモリ・停止取りこぼし）→ 下記 DA記録

### Phase 1: Python 側 — sidecar に mic モード ✅
- [x] `python/src/synchroni_note/pipeline/sidecar.py` に **mic/simulate モード追加**（DD-010 `capture_mic`＋`VadChunker`＋`_transcribe` を JSON Lines で中継）。`capture.py` は状態printを **stderr化**（stdout=JSON専用を維持）＋`paused` 対応。ruff 通過
- [x] 🔬 機械検証: `--simulate audio/sample01.wav --model base` → `meta`(mode:mic,duration70.0)→`segment`×11(seq0..10・日本語化けなし)→`done`(count11)・exit0。実 `--mic` も `echo stop |` でデバイス起動→graceful `done`(count0)・stdoutクリーン
- [x] 😈 DA批判レビュー → 下記 DA記録

### Phase 2: マイク差分の配線（3-C を再利用）✅
- [x] `app/src-tauri/src/lib.rs`: 共通 `spawn_and_relay` に集約して file/mic を切替。**`start_mic`/`pause_mic`/`resume_mic`/`stop_mic`** を追加（停止は stdin に `stop` を書く graceful 終了。ウィンドウ破棄は taskkill ツリーkill＝DA#4/新4）。`SttState` を child＋制御stdin に拡張、start 時に既存セッションを kill
- [x] `app/src/pages/S05Realtime.vue`: 録音 開始/一時停止/再開/停止＋recording/paused 状態＋mic時 meta(duration無し)対応。3-C の listen／timeline.push を再利用。dev専用「疑似マイク」（実マイク不要のsimulate）も追加。vue-tsc 通過
- [x] 🔬 機械検証（**実ウィンドウ・Playwright不可**）: **実マイクで発話→S-05 に1行ずつ表示**（ユーザー実機確認OK・証跡 [mic-live-paused.png](DD-012-1/mic-live-paused.png)）。一時停止/再開/停止が機能。制御パスはログで `ctrl pause/resume/stop`→`done`、**孫プロセス残存なし**を実測。simulate表示も確認（[mic-simulate-timeline.png](DD-012-1/mic-simulate-timeline.png)）
- [x] 😈 DA批判レビュー → 下記 DA記録

## 完了条件（DoD）

- 実ウィンドウでマイク録音開始→発話が S-05 に逐次表示され、一時停止/停止が効く。
- 停止・ウィンドウ終了で裏方プロセスが残らない。

## ログ

### 2026-06-08
- **完了**。DD-011 3-C の土台（Rust中継／画面listen／kill）に「マイク入力」と「連続収音の操作」を追加。実マイクで発話→S-05 に1行ずつ表示、一時停止/再開/停止が機能することを**ユーザー実機確認**（証跡 [mic-live-paused.png](DD-012-1/mic-live-paused.png)）。実装差分は設計どおり3点＝① sidecar に `--mic`/`--simulate`（DD-010 `capture_mic`/`VadChunker`/`_transcribe` を JSON Lines 中継。`capture.py` の状態printを stderr化＋`paused` 対応）② Rust `start_mic`＋stdin制御 `pause/resume/stop`（共通 `spawn_and_relay` に集約、`SttState` を child＋stdin に拡張、start時に既存kill）③ S-05 に録音操作UI＋recording/paused状態。検証手段＝実マイク無しの **`--simulate`**（feed_samples）＋ dev専用「疑似マイク」ボタン＋ クリック自動化が不安定なため **dev自動トリガ** と **ユーザー実機確認** で担保（クリック用 `scripts/*.ps1` は Per-Monitor-Aware V2 化）。制御パスは `[stt] ctrl pause/resume/stop`→`done`・孫プロセス残存なしを実測。ruff/vue-tsc 通過。**次=親 DD-012 の DD-012-4（追い上げ整形）等、または DD-012-2/-3 の進行を待って統合**。
- DD作成（親 DD-012 の子）。
- 重複整理（A案）: DD-011 Phase 3 が 3-A/3-B/3-C に再編され、3-C（ファイル入力での Rust中継＋S-05配線）と土台が重複していたため、本DDを**「3-C のマイク差分」に再定義**。依存を 3-C 完了に明確化し、Rust中継・listen基盤・ダミー撤去・kill の新規実装は 3-C 担当として本DDから除外（再利用）。

---

## DA批判レビュー記録

<!-- DA批判レビューの手順・品質フィルター・再チェック条件は doc/da-method.md を参照 -->

### DA批判レビュー（Phase 0–2）

**DA観点:** （マイク特有の罠／スコープの取りこぼし／長時間安定性／検証可能性）

| # | 発見した問題/改善点 | 重要度 | 再現手順（高/中は必須） | DA観点 | 対応 |
|---|-------------------|--------|----------------------|--------|------|
| 1 | **マイクデバイス選択が未配線**: スコープは「S-04/S-08 の選択値を使用（無ければ既定）」だが、設定の永続化が未実装のため**既定デバイス固定**。設定で別マイクを選んでも反映されない | 中 | S-08 でマイクを変更→反映されない | スコープ部分未達 | ⏭️ `sidecar --device`／`start_mic` 引数の口は用意済み。設定永続化を実装する段（別フェーズ）で配線。当面は既定デバイスで実用可 |
| 2 | **base モデルは実発話で誤変換が目立つ**（確認テストで「ドラえもん」→「ドライモン」等） | 低 | 実マイクで話す | モデル精度 | 想定内（疎通優先）。精度は medium 切替・用語辞書（後フェーズ／設定連携）で改善。経路の正しさが本DDの確認点 |
| 3 | **短い発話は確定まで間が要る**: VAD が `min_speech 2s ＋ silence 400ms` で区切るため、1〜2語だと出るまで待つ | 低 | 1語だけ話してすぐ止める | 体感遅延（DA-新1系） | 仕様（チャンク確定条件）。録音中表示で吸収。`max_seg`/`silence` はつまみで調整可 |
| 4 | **長時間録音の安定性は未検証**（数分以上の連続収音でメモリ増・取りこぼしの有無） | 中 | 長時間録音し続ける | 長時間安定性 | 本DDは疎通優先で短時間のみ確認。実会議級の長時間安定性は **P4-3（DD-014 Rust移植・ringbuf/backpressure）** または実会議評価で確認 |
| 5 | **停止経路が2系統**: 停止ボタン=stdin に `stop`（graceful・done を出す）／ウィンドウ破棄=taskkill ツリーkill（DA#4/新4）。一時停止中の音声はチャンカに入れず破棄（=時間も進めない） | 低 | 一時停止→停止／ウィンドウ×で閉じる | 設計確認 | 意図どおり。両経路とも **孫プロセス残存なし**を実測（`ctrl stop`→done／taskkill exit）。pause=録音しない設計 |
| 6 | **UI自動クリックがマルチモニタ混在DPIで不安定**（ウィンドウが別モニタへ移ると座標ズレ・別要素を誤クリック）＝検証ツール側の問題 | 低(ツール) | 別DPIモニタにウィンドウが移動→クリックがズレる | 検証可能性 | `scripts/{shot-window,click}.ps1` を **Per-Monitor-Aware V2** 化＋down/up間に間隔。実機検証はクリックに依存せず **dev自動トリガ＋ユーザー実機確認** で担保 |
