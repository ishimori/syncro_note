# DD-012-2: 会議終了から清書・プレビュー保存の縦串

| 作成日 | 更新日 | ステータス |
|--------|--------|------------|
| 2026-06-08 | 2026-06-08 | 完了（Phase 2・実ウィンドウ E2E OK：サンプル→清書→保存→DB completed 確認・DoD達成） |

> 親DD: [DD-012 製品化（中核機能の実装と実用化）](DD-012_製品化_中核機能の実装と実用化.md)
> アプローチ: 標準（探索的実装。既存パイプライン DD-008/DD-006 の結線が中心）

## 目的

「会議を終了」を押すと、確定タイムライン＋人間メモ＋コンテキストを **gemma4:26b** で議事録Markdownに清書し、**S-06（生成中）に進捗をストリーミング → S-07（プレビュー）で確認・保存**できるようにする。製品の中核価値（議事録が手に入る）を画面上で完結させる。

## 背景・課題

- バッチ要約は DD-008 で実証済み（`python` 側 CLI、gemma4:26b で議事録Markdown生成、品質4.5/5）。モデル切替（live退避→batchロード）は DD-006 で手順確立（`keep_alive:0`／`OLLAMA_MAX_LOADED_MODELS=1`／`/api/ps` 空確認）。
- 画面 S-06/S-07 は DD-011 でモック実装済み（進捗ステッパ・ストリーミング枠・プレビュー・保存ボタンの見た目あり）。**残るは中身との結線**。

## 検討内容

- 清書入力 = 確定テキスト（主役）＋人間メモ（最優先・全文保持）＋事前コンテキスト（基本設計の「終了後バッチに一本化」原則に従う）。
- サイドカーは Phase3 契約を拡張（`summary-*` 系イベント or 別サブコマンド）。ストリーミングは tok 単位 or 行単位を着手時に決定。
- 保存先: まず Markdownファイル、**DD-012-3 完了後に SQLite（final_minutes / status='completed'）へ**差し替え。

## スコープ

- **やる**: 終了トリガー→モデル切替→清書ストリーミング→S-06表示→S-07で最終Markdown確認・（任意）微修正・保存。
- **やらない**: マイク収音（012-1）／DBスキーマ実装の詳細（012-3）／話者・整形（012-4/5）。

## 依存

- 入力: 文字起こしテキスト（DD-011 Phase3 / DD-012-1）。
- 流用: **DD-008**（要約パイプライン）, **DD-006**（モデル切替手順）。
- 保存の本実装: **DD-012-3**（先行 or 並行。未完なら一旦ファイル保存）。

## タスク一覧

### Phase 0: 事前精査
- [x] 📋 対象パス・🔬機械検証の精査（新規: `pipeline/model_switch.py`・`pipeline/summarize_sidecar.py`、テスト2本。検証=pytest＋可能ならOllama実機）
- [x] 📐 実装前詳細化（外部I/F=サイドカー契約拡張／モデル切替 → **下記「Phase 0 設計判断」**）
- [x] 😈 Devil's Advocate（清書中のメモリ二重確保・切替失敗・中断時の状態・長文 → **下記DA記録**）

#### Phase 0 設計判断
- **別口の新規ファイルにする（sidecar.py を触らない）**: 文字起こし中継 `pipeline/sidecar.py` は DD-011 3-C が管轄・並行編集中。清書は終了後バッチで目的も起動も別なので、**`pipeline/summarize_sidecar.py` を新設**して衝突を避ける（CLIも別: 位置引数=transcript）。
- **契約は Phase3 を踏襲して拡張**（全行 `"v":1`／`type` 区別／stdoutはJSON専用）。清書系イベントは `summary-` 接頭辞で名前空間を分ける:
  `summary-meta`（開始・model/input_chars）／`summary-status`（モデル切替の段階）／`summary-progress`（**差分テキスト delta** ＝ S-06 で追記表示）／`summary-done`（全文markdown＋tokens＋elapsed）／`error`。
- **モデル切替は製品コードへ昇格**: bench の実測ロジック（`keep_alive=0`退避→`ollama.ps()`空確認→batchロード）を `pipeline/model_switch.py` に純度高く再実装。**batchをロードする前に live の消失を待つ**ことで二重常駐（メモリ二重確保・DA#1）を構造的に防ぐ。
- **テスト可能性**: Ollama 依存部（生成・ps）と、ストリーム→契約イベント変換の純ロジックを分離。後者は **Ollama 非依存で pytest** 可能にする（`stream_to_events` 純粋ジェネレータ）。

### Phase 1: Python 側 — 清書サイドカー口＋モデル切替 ✅
- [x] 📐 実装前詳細化 → 👀レビュー（Phase 0 設計判断で確定: 別口新設＋`summary-`契約＋切替昇格）
- [x] 確定テキスト＋コンテキストを受け取り gemma4:26b で清書し、進捗を JSON Lines で stream する口を追加（`pipeline/summarize_sidecar.py`、DD-008 の `stream_summarize` を流用）
  - 純ロジック分離: `stream_to_events()`（`(累積text,metrics)`→契約イベント変換）を Ollama 非依存にし pytest 可能化
- [x] モデル切替（live退避→batchロード）を DD-006 手順で製品コード化（`pipeline/model_switch.py`: `unload_model`→`wait_until_unloaded`(ps空確認)→`switch_to_batch`）。**batchロード前にlive消失を待ち二重常駐を回避**
- [x] 🔬 機械検証（pytt＋実機）:
  - `pytest tests/test_model_switch.py tests/test_summarize_sidecar.py` → **9 passed**、`ruff` 緑
  - **実機 Ollama**: `qwen3:8b` 常駐 → `summarize_sidecar audio/sample_minutes_src.txt` 実行 → status(`unloading`→`unloaded`→`loading_batch`)、progress 差分117行、done(in=311/out=113tok/7.34s)、要約/決定事項/TODO の3節を生成。**`ollama ps` が qwen→gemma に切替**を確認
- [x] 😈 DA批判レビュー（下記 Phase 1 DA記録）

### Phase 2: Rust 中継 ＋ S-06/S-07 結線
- [x] `app/src-tauri/src/lib.rs`: 終了トリガーで清書サイドカーを spawn・進捗を emit（`spawn_and_relay` を `Relay`(stt/summary)＋`StdinMode`(None/Control/Feed) に一般化。`start_summarize`(stdinで確定テキスト投入・モデルはS-08設定)・`abort_summarize` を追加。清書の `error`→`summary-error` に名前空間分離）
- [x] 保存は `create_meeting` を流用（**保存時に status='completed' で1件だけ作成**）。当初 `save_final_minutes` コマンドを追加したが、コードレビューで幽霊会議問題が判明し撤去。`db.rs` の `save_final_minutes` は予約会議（S-02発）の完了用に温存（公開は別途）
- [x] `app/src/pages/S06Generating.vue`: summary-* を listen し実進捗・実ストリーム表示（モック固定値を撤去。ステッパ=status、生成=progress、完了=done で駆動。中断=`abort_summarize`）
- [x] `app/src/pages/S07MinutesPreview.vue`: 最終Markdownを最小レンダラで表示・編集・**保存時に `create_meeting`(completed) で作成**→S-01（直接遷移ガード／二重保存ガード付き）
- [x] 画面間受け渡し `app/src/session.ts`（reactive シングルトン・清書元/会議名/結果のみ保持）。**DBへの書き込みは保存時の1回だけ**＝中断/エラー/未保存離脱でも幽霊会議が出ない。保存で completed 会議がカレンダーに出現＝一周
- [x] 静的検証: `cargo test`（13緑・コンパイル緑）/ `vue-tsc --noEmit`（緑）/ `pytest`（67緑）/ `ruff`（緑）
- [x] 🔬 機械検証（**実ウィンドウ**）: S-05「会議を終了」→S-06進捗→S-07に議事録→保存を実機で通過（下記ログ参照）。**OK**
- [x] 😈 DA批判レビュー（下記 Phase 2 DA記録）

## 完了条件（DoD）

- 実ウィンドウで「会議を終了→清書ストリーム→S-07に議事録Markdown→保存」が通る。
- 清書中に live/batch モデルが正しく切り替わる（`/api/ps` で確認）。

## ログ

### 2026-06-08
- DD作成（親 DD-012 の子）。DD-008/DD-006 を S-05→S-06→S-07 に結線する「議事録が手に入る」縦串として起票。
- **Phase 0 完了**: 設計判断（別口新設で sidecar.py を触らない／`summary-`契約／切替の製品コード化／純ロジック分離でテスト可能化）と DA(4件) を記録。
- **Phase 1 完了（Python 清書サイドカー＋モデル切替）**: 新規 `pipeline/summarize_sidecar.py`・`pipeline/model_switch.py`、テスト2本（`test_summarize_sidecar.py`/`test_model_switch.py`）。pytest **9件緑**＋ruff緑。**実機 Ollama で end-to-end 検証**: 切替(qwen→gemma)・stream(117差分)・3節議事録生成・`ollama ps` 切替を確認。検証用素材 `audio/sample_minutes_src.txt` を追加。
- **Phase 2 実装完了（Rust中継＋S-06/S-07結線・静的検証緑）**: 縦串「会議を終了→清書ストリーム→S-07に議事録→保存」をコード結線。
  - Rust: `spawn_and_relay` を `Relay`/`StdinMode` で一般化し、`start_summarize`(stdin投入・S-08のbatch/liveモデル反映)・`abort_summarize`・`save_final_minutes`(db_commands) を追加・登録。
  - Front: `session.ts` で S-05→S-06→S-07 を受け渡し。S-05「会議を終了」で会議を `generating` 作成 → S-07保存で `save_final_minutes`→`completed`（カレンダーに完了会議として出現＝一周）。S-06 は summary-* で実進捗、S-07 は最小Markdownレンダラ＋保存。
  - Python: 清書出力の literal `\n`/`\t` を `normalize_minutes` で正規化（Phase1 DA#2 を消化）。`summary-done` の markdown に適用。pytest 2本追加（計 **67緑**）。
  - 静的検証緑: `cargo test` 13 / `vue-tsc` / `pytest` 67 / `ruff`。
  - **残（DoD）**: 実ウィンドウ E2E（mic/サンプル→gemma 清書→保存）。Playwright では Tauri invoke が動かず自動実行不可 → Ollama 起動＋gemma4:26b で手動確認が必要。→ **下記で実施・OK**。
- **追加実装: 証跡タイムラインの永続化（上記 DA#6 を解消）**: 「保存会議は timeline 未保存で S-03 詳細が空」をユーザー要望で当日解消。録音の確定文字起こし＋人間メモを構造化して持ち回り（`session.ts` の `TimelineRow`）、保存時に `create_meeting` を拡張して **会議＋参加者＋timeline を1トランザクション**で書き込む（`db_commands.rs`、FK順 meetings→participants→timeline_elements）。`t_ms` は S-05 で "mm:ss"→ms 復元、`speaker_id` は話者分離未実装のため null（S-03 では「話者 ?」表示）。`api.ts` の `createMeeting` は timeline 引数をデフォルト `[]` にし S-02 は無変更。実機検証: 新規録音→保存→S-03「元タイムライン（証跡）」に文字起こし各行＋📝人間メモが時刻つきで表示。**DB確認**: 当該会議に timeline_elements **14行（ai 12＋memo 2）**、seq昇順・t_ms(0/7000/14000/23000/32000)保持、実装前に保存した会議は 0 行で無影響。検証: `vue-tsc` 緑／`tauri dev` の自動 rebuild 緑（44.56s で再起動）。
  - スコープ判断: 清書入力は当面「S-05 画面上の確定タイムライン＋人間メモ」を stdin で渡す（DB `timeline_elements` 経由は録音時の永続化＝DD-012-1/3 の範囲で別途）。会議行の作成タイミングは当初「終了時」だったが、**下記コードレビューで「保存時の1回」に変更**（録音開始での active 化は S-04 scope-out のまま）。
- **コードレビュー反映（自己レビュー：バグ3件＋設計見直し）**:
  - 🔴 修正3件: ①S-06 で生成途中に画面を離れると清書(gemma)が裏で走り続け 17GB を数分占有 → 離脱時に `abort_summarize` で停止。②S-05「会議を終了」失敗が `status='error'` 未設定で画面に出ず黙って失敗 → エラー表示を出す。③「会議を終了」連打で会議が二重作成されるすき間（ロックが `await` の後）→ 押下直後に `ending` ロック。
  - 🟡 設計見直し（幽霊会議の根治）: 「終了時に `generating` 作成→保存で `completed`」だと保存しない限り `generating` が残り続ける（中断/エラー/未保存離脱。schema の `aborted` も未使用だった）。**会議の作成を「保存時の1回だけ」に変更**（S-07 で `create_meeting(status='completed')`）。未保存では DB に何も書かれず幽霊が構造的に出ない。S-05 は会議を作らず清書元/会議名のみ session へ。一旦追加した `save_final_minutes`(db_commands/api/登録) は撤去（`db.rs` の関数は温存）。S-07 保存に二重押下ガードを追加。
  - 再検証: `cargo test` 13緑 / `vue-tsc` 緑 / `pytest` 67緑。
  - 既知の割り切り（バグでない）: 保存会議は timeline 未保存のため S-03 詳細でタイムラインが空（録音時永続化=DD-012-1/3 で対応）／最小レンダラは `**強調**`・表を生表示。
- **実ウィンドウ E2E 検証 OK（DoD 達成）**: 実機（Tauri窓＋Ollama）で一周を確認。サンプル音声→文字起こし(951字/13セグ)→「会議を終了」→モデル切替(qwen退避→gemma展開・UIステッパ＆ログで確認)→gemma4:26b 清書(**115tok / 14.1tok/s / 8.2s**)→S-07 で3節Markdownを整形表示→保存。DB(`synchroni_note.dev.sqlite`)に **status='completed'・batch_model='gemma4:26b'・generation_seconds=8・final_minutes 205字** の会議行(UUID `d98521d3…`)が書き込まれたことを確認。relay ログは summary-meta→status→progress(多数)→done→stdout closed、エラーなし。`\n` 文字化けも無し（normalize 効）。

---

## DA批判レビュー記録

### Phase 0 DA批判レビュー

**DA観点:** 清書（長時間・モデル切替・契約拡張）で壊れやすい所は？

| # | 発見した問題/改善点 | 重要度 | 再現手順 | DA観点 | 対応 |
|---|-------------------|--------|---------|--------|------|
| 1 | 清書中のメモリ二重確保（live と batch が同時常駐） | 高 | batchロード前にliveを退避しないと17GB+6GBで圧迫 | メモリ二重確保 | `switch_to_batch` で**batchロード前にlive消失を待つ**順序保証。`test_switch_to_batch_*` で通知順を固定 |
| 2 | sidecar.py を直接拡張すると 3-C と編集衝突 | 中 | 同ファイルを2セッションで編集 | 並行作業の衝突 | **別口 `summarize_sidecar.py` を新設**。sidecar.py に触れない |
| 3 | stdout に清書ログが混じり Rust パースが汚れる | 中 | print デバッグが混入 | 契約汚染 | stdoutはJSON専用・ログは stderr（既存サイドカー方針を踏襲） |
| 4 | 切替失敗（ps が空にならない）の検知不在 | 中 | Ollama 応答遅延で `wait_until_unloaded` が timeout | 失敗の握り潰し | `switch_to_batch` が False を返す口を用意（**Phase 1 DA#1 で消費側対応へ繰越**） |

### Phase 1 DA批判レビュー

**DA観点:** 実機で通ったが「本番の別ケースで崩れる」のはどこか？

| # | 発見した問題/改善点 | 重要度 | 再現手順 | DA観点 | 対応 |
|---|-------------------|--------|---------|--------|------|
| 1 | 切替が timeout（`switch_to_batch`→False）でも main は無視して生成へ進む。liveが残ったまま26bロードで**二重常駐**しうる | 中 | Ollama 退避が30s超 | 失敗時フォールバック不在 | 暫定可（成功時は安全）。要対応: main で False を受けたら `error`/`summary-status:"switch_failed"` を出し中断 or 警告。Phase 2 配線時に実装 |
| 2 | **gemma が議事録中に literal `\n`（バックスラッシュ+n）を混ぜる**ことがある（実機出力で確認: 「## 要約\n認証…」やTODO各項） | 中 | 26bで清書→Markdown内に `\n` 文字列が残る | 表示崩れ | S-07 表示前に `\n`→改行へ正規化、もしくはプロンプトで明示。Phase 2 の表示口で対処（要 normalize 関数＋テスト） |
| 3 | 長い会議は context(4096)超で末尾が切れ、清書が尻切れ | 中 | 長時間会議の全文を一括投入 | 文脈窓あふれ | 暫定: 短中会議で成立。長文は map-reduce（分割要約→統合）が必要 → 別タスク化候補。本Phaseのスコープ外として明記 |
| 4 | `--no-switch` 時に batch 未ロードだと初回TTFTが長く、UIが無反応に見える | 低 | batch 未常駐で `--no-switch` 実行 | 体感の空白 | `summary-status:"loading_batch"` を切替なしでも出すか、S-06 で「準備中」を保持 |

### Phase 2 DA批判レビュー

**DA観点:** UI結線（実ウィンドウの別ケース）で崩れる所は？ 実装後の自己コードレビューで摘出し、🔴は即修正。

| # | 発見した問題/改善点 | 重要度 | 再現手順 | DA観点 | 対応 |
|---|-------------------|--------|---------|--------|------|
| 1 | 生成途中に画面を離れると清書(gemma)が裏で走り続け 17GB を数分占有 | 中 | S-06 生成中に左メニューで別画面へ | 後始末漏れ | onUnmounted で `!done` なら `abort_summarize`＋session破棄。**済** |
| 2 | 「会議を終了」失敗が画面に出ない（`status='error'` 未設定で表示条件と不一致） | 中 | 会議終了処理でエラー | 失敗の握り潰し | 失敗時に `status='error'` を設定し表示。**済** |
| 3 | 「会議を終了」連打で会議が二重作成（`ending` ロックが `await` の後） | 中 | ボタンを素早く2回 | 二重サブミット | 押下直後に `ending=true`。**済**（さらに作成タイミング変更で根治） |
| 4 | **未保存の `generating` 会議が幽霊化**（中断/エラー/保存せず離脱で残る・`aborted` 未使用） | 中 | 清書後に保存せずカレンダーへ | 中途半端な永続化 | **作成を保存時の1回に変更**（S-07 で completed 作成）。未保存では DB に書かない＝根治。**済** |
| 5 | S-07 保存の二重押下で会議が二重作成し得る（insert 化に伴い顕在化） | 中 | 保存ボタン連打 | 二重サブミット | `if(saving) return`＋ボタン disable。**済** |
| 6 | 保存会議は timeline 未保存 → S-03 詳細でタイムラインが空 | 低 | 完了会議を S-03 で開く | 入力源未永続化 | **解消済み**（ユーザー要望で当日対応）: 保存時に `create_meeting` を拡張し timeline も書き込む（下記ログ）。実機＋DBで14行確認 |
| 7 | 最小Markdownレンダラは `**強調**`/表/コードブロック非対応（生表示） | 低 | gemma が `**`/表を出力 | 表示の欠落 | 見出し/箇条書き/チェックは描画。リッチ描画は後続の磨き込み |
