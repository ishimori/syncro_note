# DD-016-1: 会話エリアの独立スクロール＋ヘッダ/録音操作バー固定

| 作成日 | 更新日 | ステータス |
|--------|--------|------------|
| 2026-06-09 | 2026-06-09 | 未着手 |

> 親: [DD-016](DD-016_S-05リアルタイム画面の改善_録音中の右パネル編集と会話エリア独立スクロールとヘッダ固定.md)。アプローチ: モック先行（合意済みモック `DD-016/mock/S-05_realtime_v2.html` が正）。FE完結・**独立して出荷可能（先行推奨）**。

## 目的

S-05 リアルタイム議事録で、会話が長くなっても見やすいレイアウトにする。

1. **会話記録エリア（確定タイムライン）だけを独立スクロール**（ページ全体は伸びない）。
2. 新しい発話が来たら**最下部へ自動追従**（ただしユーザーが上方を読んでいる間は追従しない）。
3. **ヘッダ（経過・終了等）と録音操作バー・案内バナーを固定**。

## 背景・課題

現状は `q-page` がタイムラインぶん伸びて**ページごとスクロール**し、ヘッダ・録音操作ボタン・人間メモ欄が画面外へ流れる（親DD背景参照）。

## 検討内容

- Quasar の `.row`/`.column` は `flex-wrap:wrap` を含むため、縦方向の高さ配分（flex-grow）が壊れ、子が縮まずスクロールが発生しない（モック検証で実証）。→ **nowrap の専用クラス**（`.fill-col`/`.fill-row`）で高さを配分し、各 flex 階層に `min-height:0` を付ける。
- ページ自体のスクロールは `html,body{overflow:hidden}` 相当で止め、タイムライン領域のみ `overflow-y:auto`。
- 自動追従: 新規セグメント push 後、**スクロール位置が最下部付近のときだけ** `scrollTop=scrollHeight`（上方閲覧中は維持）。

## 決定事項

- 合意済みモックのレイアウトをそのまま `S05Realtime.vue` に移植する。人間メモ（右ペイン）もビューポート内に収め、必要なら自身でスクロール。

## タスク一覧

### Phase 1: 実装
- [ ] 📐 **実装前詳細化** → 👀 レビュー（触る要素・flex階層・自動追従の判定条件）
- [ ] `app/src/pages/S05Realtime.vue`: メインを「固定ヘッダ＋固定操作バー＋タイムライン内部スクロール」へ。`.row/.column` の flex-wrap:wrap を避け nowrap 専用クラスで高さ配分（モック知見）
- [ ] `app/src/pages/S05Realtime.vue`: 新規セグメント時、最下部付近のときのみ自動追従（上方閲覧中は抑止）
- [ ] 🔬 **機械検証**: `cd app && npx vue-tsc --noEmit`（型エラー0）
- [ ] 🖥️ **実機確認**: `node scripts/tauri-cdp.mjs` で録音→長尺タイムライン。ページ全体が動かずタイムライン内のみスクロール／ヘッダ・操作バー固定／自動追従をスクショ確認し `DD-016-1/` にエビデンス
- [ ] 😈 **DA批判レビュー**（自動追従の取りこぼし・小さいウィンドウでの収まり・右ペインメモの高さ・最低1件）

## ログ

### 2026-06-09
- 親DD-016から分割して作成。実装は合意済みモックのレイアウト移植。

---

## DA批判レビュー記録

<!-- 手順・品質フィルターは doc/da-method.md を参照 -->

### Phase 1 DA批判レビュー

**DA観点:** （高さ配分・スクロール追従で最も壊れやすい点は？）

| # | 発見した問題/改善点 | 重要度 | 再現手順（高/中は必須） | DA観点 | 対応 |
|---|-------------------|--------|----------------------|--------|------|
| 1 | (具体的に記述) | 高/中/低 | (高/中: 操作→結果) | (DA観点) | ✅修正済/⏭️別DD/❌不要 |
